# ⌐◨-◨


# Changes
- Manager allows multiple tokenImpl to exist: `Owner` can regist new implementations & pass implementation id on `deploy` function.
- Permissioned new DAO deploymen, only the `Owner` can call the `deploy` function on `Manager`